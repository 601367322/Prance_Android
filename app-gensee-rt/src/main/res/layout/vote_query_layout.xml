<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical" >

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal" >

        <TextView
            android:id="@+id/vote_query_votename_tv"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:padding="@dimen/vote_margin_btn"
            android:singleLine="true"
            android:layout_weight="1"
            android:textSize="@dimen/vote_title" >
        </TextView>

        <Button
            android:id="@+id/vote_query_close_btn"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/close" >
        </Button>
    </LinearLayout>

    <ExpandableListView
        android:id="@+id/vote_query_expandlv"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:cacheColorHint="@android:color/transparent"
        android:childDivider="@color/vote_lv_divide"
        android:divider="@color/vote_lv_divide"
        android:groupIndicator="@null" >
    </ExpandableListView>

</LinearLayout>